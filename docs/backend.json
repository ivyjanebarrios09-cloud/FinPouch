
{
  "entities": {
    "user": {
      "title": "User",
      "description": "A user of the application.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        }
      },
      "required": ["uid", "email"]
    },
    "walletActivity": {
      "title": "Wallet Activity",
      "description": "A record of a wallet opening event.",
      "type": "object",
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "The time the wallet was opened."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who performed the activity."
        }
      },
      "required": ["timestamp", "userId"]
    },
    "device": {
      "title": "Device",
      "description": "A device registered by a user.",
      "type": "object",
      "properties": {
        "deviceId": {
          "type": "string",
          "description": "The unique ID of the device."
        },
        "name": {
            "type": "string",
            "description": "A user-friendly name for the device."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns the device."
        },
        "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "The time the device was registered."
        }
      },
      "required": ["deviceId", "name", "userId", "createdAt"]
    }
  },
  "auth": {
    "providers": ["email"]
  },
  "firestore": {
    "/users/{userId}/walletActivity/{activityId}": {
      "schema": {
        "$ref": "#/entities/walletActivity"
      },
      "description": "Stores wallet activity for each user."
    },
    "/users/{userId}/devices/{deviceId}": {
      "schema": {
        "$ref": "#/entities/device"
      },
      "description": "Stores devices registered by each user."
    }
  }
}
